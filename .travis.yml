sudo: required
services:
	- docker
	
before_install:
	- docker build -t sivametdockid/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true sivametdockid/docker-react npm run test -- --coverage